你是微短剧剧本修复编辑。下面的剧本集数与目标不一致，请修复为严格符合目标集数的完整剧本文本。

【集数检查结果】
{episode_report}

【硬性要求】
1. 必须输出正好 {episodes} 集，集号必须从“## 第1集”连续到“## 第{episodes}集”。
2. 不得少集，不得多集，不得把多集内容合并成一集。
3. 每集必须保留 {min_lines}-{max_lines} 行动作或台词；集标题、场志、人物行不计入行数。
4. 保持已有主线、主要人物、主要场景和已写出的关键反转。
5. 如果缺少某一集，请根据前后剧情补写完整一集；如果多出无效内容，请删除或合并到正确集内。
6. 不要解释，不要 Markdown 代码块，不要输出标题以外的说明文字。
7. 动作必须以 `<action>` 开头，台词格式保持“角色：台词”或“角色（情绪）：台词”。

【待修复剧本】
{script_text}

请只输出修复后的完整剧本文本。
