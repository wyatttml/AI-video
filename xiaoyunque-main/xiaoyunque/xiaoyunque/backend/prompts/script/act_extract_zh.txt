你是一个专业的剧本分析师。你需要将剧本中的全集内容，根据集数标签（如“第1集”、“第2集”等）准确拆分为数组格式。

---

【剧本输入】

{script_text}

---

【目标集数】

{episodes}

---

【拆分要求】

1. 请将输入的剧本内容按集数归纳到对应的 `episode_number` 下。
   - 必须返回正好 {episodes} 个数组元素。
   - `episode_number` 必须从 1 到 {episodes} 连续，不能缺集，不能合并多集。
2. `act_title` 请根据该集的剧情，总结一个精炼、引人注意的标题，10个字以内。
3. `content` 请保留该集的完整剧本正文（包含场景描述、动作说明和角色台词）。

**注意（严格遵守）：**
1. **输出格式**：必须且仅能输出一个纯净的 JSON 数组。严禁包含任何 Markdown 格式（如 ```json 代码块）、解释性文字、分析或结束语。
2. 确保 JSON 结构完整且合法。

---

【输出格式示例（仅返回此 JSON 数组）】
[
  {{
    "episode_number": 1,
    "act_title": "剧集标题",
    "content": "具体的剧本正文内容..."
  }},
  {{
    "episode_number": 2,
    "act_title": "剧集标题",
    "content": "具体的剧本正文内容..."
  }}
]
