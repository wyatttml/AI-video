你是一个专业的剧本分析师。你需要将续写的剧本内容，根据集数标签（如“第{start_episode_num}集”等）准确拆分为 JSON 数组。

---

【剧本输入】

{sequel_script}

---

【拆分要求】

1. 请将输入的剧本内容按集数归纳到对应的 `episode_number` 下。本次续写从第 {start_episode_num} 集开始。
2. `act_title` 请总结一个精炼的标题，20个字以内。
3. `content` 请保留该集的完整剧本正文（包含场景描述、动作说明和角色台词）。

**注意（严格遵守）：**
1. **输出格式**：必须且仅能输出一个纯净的 JSON 数组。严禁包含任何 Markdown 格式（如 ```json 代码块）、解释性文字或结束语。
2. 确保 JSON 结构完整且合法。

---

【输出格式示例（仅返回此 JSON 数组）】
[
  {{
    "episode_number": {start_episode_num},
    "act_title": "剧集标题",
    "content": "具体的剧本正文内容..."
  }}
]
