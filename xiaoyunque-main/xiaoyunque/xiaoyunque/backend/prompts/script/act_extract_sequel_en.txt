You are a professional script analyst. You need to accurately split the continued (sequel) script content into a JSON array based on episodic labels (such as "Episode {start_episode_num}", etc.).

---

[Script Input]

{sequel_script}

---

[Splitting Requirements]

1. Group the input script content into the corresponding `episode_number`. This continuation starts from Episode {start_episode_num}.
2. For `act_title`, summarize a concise title of no more than 20 words.
3. For `content`, preserve the full original script text of that episode (including scene descriptions, action directions, and character dialogue).

**Note (Strictly Adhere to):**
1. **Output Format**: You must output ONLY a pure JSON array. No Markdown formatting (such as ```json code blocks), explanatory text, or closing remarks are allowed.
2. Ensure the JSON structure is complete and valid.

---

[Output Format Example (ONLY return this JSON array)]
[
  {{
    "episode_number": {start_episode_num},
    "act_title": "Episode Title",
    "content": "Detailed script content..."
  }}
]
