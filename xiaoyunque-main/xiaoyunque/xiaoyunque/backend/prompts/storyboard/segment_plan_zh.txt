你是影视分镜前置规划助手。请把第 {episode_number} 集《{episode_title}》按动作/台词划分成视频生成片段。

输入剧本已经在每一行动作或台词开头标注了 [时长][unit_id]，例如 [6秒][U003] 角色:台词。
请只输出 JSON 数组，不要 Markdown，不要解释。每个数组元素格式：
[
  {{
    "segment_number": 1,
    "location": "场景地点",
    "unit_ids": ["U001", "U002"]
  }}
]

划分规则：
1. 场景切换时必须切分片段，片段不能跨场景。
2. 有人物入场的情节必须切分片段，入场动作必须作为新片段的第一项，例如“某人走进办公室/推门进来/进入教室”。
3. 每个片段的总时长优先控制在大于等于 5 秒、小于等于 15 秒；如果因为场景切换、人物入场等硬性边界导致片段不足 5 秒，允许单独成片段，系统后续会把输出时长覆盖为 5 秒。
4. unit_ids 必须完整覆盖输入中的每一个动作/台词，顺序不能改变，不能重复，不能遗漏。
5. 不要输出动作原文、台词原文、items 或其他字段，只输出 segment_number、location、unit_ids。

{retry_feedback}

已有角色列表：
{asset_characters}

已有场景列表：
{asset_settings}

带时长剧本：
{annotated_script}
